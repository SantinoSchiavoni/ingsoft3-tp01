# PostgreSQL para Docker Compose
POSTGRES_USER=orderflow
POSTGRES_PASSWORD=orderflow
POSTGRES_DB=orderflow

# Backend local: PostgreSQL de Docker publicado en el host
DATABASE_URL="postgresql://orderflow:orderflow@localhost:5434/orderflow?schema=public"
